# ===== UCM-AI docker-compose .env (example) =====
# Backend (FastAPI)
DATABASE_URL=postgresql+psycopg2://ucm:ucm@db:5432/ucmdb
MODEL_SVC_URL=http://model_service:9000
ADMIN_API_KEY=dev-admin-key
PRIVACY_LOGGING=true

# Frontend (Next.js)
BACKEND_BASE_URL=http://backend:8000
NEXT_PUBLIC_BASE_URL=http://localhost:3000
# The admin key is also read by the frontend server-side proxy route:
#   /app/api/admin/audit/route.ts
# It is NOT sent to the browser, but do not use production secrets here.
# ADMIN_API_KEY=dev-admin-key
